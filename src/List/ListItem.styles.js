import styled from 'styled-components';

const abstand = '5px';

export const Li = styled.li`
  border: 1px solid hotpink;
  margin: ${abstand};
  padding: ${abstand};
  border-radius: ${abstand};
`;
